<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rnbbusiness.newbase.security.dao.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.rnbbusiness.newbase.security.dao.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
    </resultMap>


    <select id="findByName" resultMap="UserResultMap" parameterType="java.lang.String">
        select
          `id`, `username`,`password`
        from
          `sys_user`
        where
          `username` = #{username}
    </select>

    <select id="findRoles" resultType="java.lang.String">
        select
          sys_role.role_code
        from
          sys_role
        where
          sys_role.`id` in (
          select
            sys_user_role.role_id
          from
            sys_user_role
          where
            sys_user_role.user_id = #{userId}
          )
    </select>
</mapper>